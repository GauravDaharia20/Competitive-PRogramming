else if (pq.size() == 3)
        // {
        //     while (!pq.empty() && i != arr.size() - 1)
        //     {
        //         fl = max(fl, pq.top());
        //         pq.pop();
        //         sl = max(sl, pq.top());
        //         pq.pop();
        //         tl = max(tl, pq.top());
        //         pq.pop();

        //         ans.push_back(fl * sl * tl);
        //         pq.push(sl);
        //         pq.push(tl);
        //     }
        // }